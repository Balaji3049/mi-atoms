import React from "react";
import MUIToolbar from "@material-ui/core/Toolbar";

export const Toolbar = (props) => {
    return (
        <>
            <MUIToolbar {...props} />
        </>
    );
};
