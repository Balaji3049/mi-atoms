import React from "react";
import MUIMenuItem from "@material-ui/core/MenuItem";

export const MenuItem = (props) => {
    return (
        <>
            <MUIMenuItem {...props} />
        </>
    );
};
