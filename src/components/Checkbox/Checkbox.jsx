import React from "react";
import MUICheckbox from "@material-ui/core/Checkbox";

export const Checkbox = (props) => {
    return (
        <>
            <MUICheckbox {...props} />
        </>
    );
};
