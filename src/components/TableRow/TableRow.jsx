import React from "react";
import MUITableRow from "@material-ui/core/TableRow";

export const TableRow = (props) => {
    return (
        <>
            <MUITableRow {...props} />
        </>
    );
};
